# Bergamot Admin

Initial admin console scaffold for Bergamot parity work.

- Serves a lightweight browser UI on `http://localhost:9101`
- Reads Janus instance config and feature manifests
- Establishes the runtime boundary for later user, guild, report, audit-log, and moderation tooling
